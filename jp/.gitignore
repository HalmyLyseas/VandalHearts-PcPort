# ---- generated by the build pipeline; never committed ----
/asm/
/assets/
/build/
*.o
*.d
__pycache__/

# splat-generated build inputs (regenerated from SLPM_860.07.yaml by `make extract`)
/SLPM_860.07.ld
/undefined_syms_auto.txt
/undefined_funcs_auto.txt

# Symlink so `#include "assets/X.inc"` resolves via the -Iinclude search path
# (assets/ itself is ignored above). Recreated by tools/gen_assets_inc.py.
/include/assets

# ---- copyrighted / proprietary: MUST NEVER be committed ----
# The base executable, from your own legally-owned copy of the game.
/SLPM_860.07

# Sony PsyQ SDK headers (v3.3 SDK mirror, unclear redistribution rights) — local build use only.
# Supply your own; see README "What you must supply".
/include/PsyQ/

# Toolchain: old-gcc cc1 binaries + maspsx. Large and re-fetchable;
# external/references lists the upstream URLs.
/external/
